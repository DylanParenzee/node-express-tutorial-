// Modules
const superSecret = "shhhh this is a secret";
const dylan = "Dylan";
const susan = "Susan";

const sayHi = (name) => {
  console.log(`${name}`);
};

sayHi("John");
sayHi(dylan);
sayHi(susan);
